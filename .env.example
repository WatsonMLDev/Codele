# API Configuration
# Point this to your backend API URL
# If running locally with a proxy, this might be just /api/v1
# If running separate frontend/backend, use the full URL: http://localhost:8080/api/v1

VITE_API_BASE_URL=http://localhost:8080/api/v1

# Site URL (used for canonical links, OG tags, etc.)
VITE_SITE_URL=https://codele.watsonml.dev

# For Cloudflare Pages production deployment, set the environment variable
# in the Cloudflare dashboard under Workers & Pages > Settings > Environment Variables:
# VITE_API_BASE_URL=https://your-api-domain.com/api/v1
